import { StyleSheet } from "react-native"

export const HeaderStyles = StyleSheet.create({
    container: {
        padding: 16,
        backgroundColor: "#000",
    },
    title: {
        fontSize: 18,
        fontWeight: "bold",
        color: "#fff"
    }
})